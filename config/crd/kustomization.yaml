# Lets the CRD be consumed straight from the repository at a tag, without
# cloning and without a release asset:
#
#   resources:
#     - github.com/AlessandroZanatta/gatus-sidecar/config/crd?ref=v0.1.1
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - bases/gatus.kalexlab.xyz_endpointtemplates.yaml
